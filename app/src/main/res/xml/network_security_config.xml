<?xml version="1.0" encoding="utf-8"?>
<!--
  Network Security Config
  - Enforces HTTPS for all external connections
  - Pins Cloudflare DoH certificate (optional, can be removed if not using DoH)
  - Allows cleartext only for localhost (dev/testing)
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Allow cleartext for localhost/emulator testing only -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
